/* Shared runtime configuration for local preview and production deployment. */
window.PRJ008_CONFIG = Object.freeze({
  // Change this one value when the FastAPI service is deployed.
  apiBaseUrl: "https://robin1209-portfolio.onrender.com",
});

/* Shared runtime configuration for the PRJ008 Admin Console. */
window.PRJ008_ADMIN_CONFIG = Object.freeze({
  // Keep this aligned with the deployed FastAPI service.
  apiBaseUrl: "https://robin1209-portfolio.onrender.com",
});
